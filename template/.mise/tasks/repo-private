#!/bin/bash
# mise description="Create a private GitHub repo and push main"

set -euo pipefail

REPO="${1:?Usage: mise run repo-private <owner/repo>}"

gh repo create "$REPO" --private --source=. --push
echo ""
echo "Private repo created and main branch pushed."
echo "  https://github.com/$REPO"
