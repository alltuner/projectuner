#!/bin/bash
# ABOUTME: Removes a git worktree and its directory.
# ABOUTME: Usage: wt-rm <branch-name>

set -euo pipefail

BRANCH="${1:?Usage: wt-rm <branch-name>}"

git worktree remove "$BRANCH"
echo "Removed worktree '$BRANCH'"
